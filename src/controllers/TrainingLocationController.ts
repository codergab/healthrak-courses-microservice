import { Request, Response, NextFunction } from 'express';

export default class TrainingLocationController {
  static async getTrainingLocations(
    req: Request,
    res: Response,
    next: NextFunction
  ) {}
}
